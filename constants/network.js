// Definição de constantes para redes
const NETWORK = {
  eth: "eth",
  sol: "sol",
  optimism: "optimism", 
};

// Exporta a constante NETWORK para ser utilizada em outros módulos
module.exports = {
  NETWORK,
};
